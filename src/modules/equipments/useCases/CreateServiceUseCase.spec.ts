import { CreateServiceUseCase } from './createService/CreateServiceUseCase';

describe('Create Service', () => {
  it('should be able to create a new service', () => {
    const createService = new CreateServiceUseCase();
  });
});
